document.querySelectorAll(".leaderboard-button").forEach(btn => btn.addEventListener("click", showLeaderboard));

function showLeaderboard() {
  // Assuming showLeaderboard is a function that displays the leaderboard
  // You can add the logic to display the leaderboard here
}
